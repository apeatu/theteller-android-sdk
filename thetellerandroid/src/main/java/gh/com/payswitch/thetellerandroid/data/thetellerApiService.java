package gh.com.payswitch.thetellerandroid.data;

import android.util.Base64;

import java.util.List;
import gh.com.payswitch.thetellerandroid.thetellerConstants;
import retrofit2.Call;
import retrofit2.http.Body;
import retrofit2.http.Headers;
import retrofit2.http.GET;
import retrofit2.http.POST;

public interface thetellerApiService {

//            'Content-Type: application/json'

//             @POST("/v1.1/transaction/process")

//    public okhttp3.Response intercept(Chain chain) throws IOException {
//        okhttp3.Request request = chain.request();
//        Headers headers = request.headers().newBuilder().add("Authorization", Base64.encode("your apiuser": thetellerConstants.API_KEY)).build();
//        request = request.newBuilder().headers(headers).build();
//        return chain.proceed(request);
//    }
}
